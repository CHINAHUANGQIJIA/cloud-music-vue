import Vue from 'vue'
import Vuetify from 'vuetify/lib'

Vue.use(Vuetify)

// const opts = {}

export default new Vuetify({
  icons: {
    iconfont: 'mdiSvg' // 'mdi' || 'mdiSvg' || 'md' || 'fa' || 'fa4' || 'faSvg'
  }
})
